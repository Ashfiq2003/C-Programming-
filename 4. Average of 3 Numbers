#include<stdio.h>
   void main()    
	{        
	   float a, b ,c, avg;        
	   printf("Input the Value of a,b,c:");        
	   scanf("%f %f %f",&a,&b,&c);        
	   avg= (a+b+c)/3;      
	   printf("The Average: %f",avg);  
  }
